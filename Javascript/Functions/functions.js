function favoriteAnimal(animal) {
    return animal + " is my favorite animal!"
}


//  
function add7(numberIn) {return numberIn + 7}
//
let multiply = (in1,in2) => in1*in2;
//
function capitalise(stringInput) {
    let stringOutput = stringInput.charAt(0).toUpperCase() + stringInput.slice(1)
    return stringOutput
}
//
let lastLetter = word => word.slice(-1)
//
console.log(lastLetter("hello"))